eureka:
  instance:
    preferIpAddress: true
    hostname: ${EUREKA_SERVER_CONTAINER_NAME}
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${EUREKA_SERVER_CONTAINER_NAME}:${server.port}/eureka

server:
  port: ${EUREKA_SERVER_PORT}